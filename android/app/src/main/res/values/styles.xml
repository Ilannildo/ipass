<resources>
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:textColor">#000000</item> -->
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="android:statusBarColor">@color/md_theme_light_background</item>
        <item name="android:textColor">@color/md_theme_light_onPrimaryContainer</item>
        <item name="android:navigationBarColor">@color/md_theme_light_background</item>

        <!-- Customizando alert -->
        <item name="android:alertDialogTheme">@style/AlertDialogStyle</item>
    </style>

    <style name="AlertDialogStyle" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="android:background">@color/md_theme_light_surface</item>
        <item name="android:colorPrimary">@color/md_theme_light_onPrimaryContainer</item>
        <item name="android:colorAccent">@color/md_theme_light_outline</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
    </style>

    <!-- Negative button alert-->
    <style name="NegativeButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/md_theme_light_primary</item>
    </style>
    
    <!-- Positive button alert -->
    <style name="PositiveButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/md_theme_light_primary</item>
    </style>

    <style name="BootTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:background">@drawable/bootsplash</item>
    </style>

</resources>