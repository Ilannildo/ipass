import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 32,
    borderRadius: 8,
    paddingLeft: 8,
    alignItems: 'center',
    flexDirection: 'row',
  },
  label: {
    fontSize: 14,
    fontWeight: '500',
    lineHeight: 20,
    letterSpacing: 0.1,
    height: '100%',
    // width: '100%',
    textAlign: 'center',
    textAlignVertical: 'center',
    marginLeft: 8,
    marginRight: 16,
  },
});
